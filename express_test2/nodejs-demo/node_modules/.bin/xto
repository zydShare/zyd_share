#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_xto@1.1.7@xto/bin/xto.js" "$@"
  ret=$?
else 
  node  "$basedir/../_xto@1.1.7@xto/bin/xto.js" "$@"
  ret=$?
fi
exit $ret
